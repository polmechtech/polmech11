import { getGearboxOffers, getOfferPath, getProductDescription, getTrendEcoPrice } from "@/lib/allegroOffers";
export const dynamic = "force-dynamic";
const esc = (value: unknown) => String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
export async function GET() { const products = await getGearboxOffers(); const items = products.filter((p) => p.id && p.name && p.price).map((p) => `<item><g:id>${esc(p.id)}</g:id><title>${esc(p.name)}</title><description>${esc(getProductDescription(p).slice(0,5000))}</description><link>https://polmech.tech${getOfferPath(p)}</link>${p.image ? `<g:image_link>${esc(p.image)}</g:image_link>` : ""}<g:availability>${p.stock > 0 ? "in_stock" : "out_of_stock"}</g:availability><g:price>${getTrendEcoPrice(p)} ${p.currency || "PLN"}</g:price><g:condition>new</g:condition><g:brand>POLMECH.TECH</g:brand><g:product_type>Łuparki przekładniowe do drewna</g:product_type><g:identifier_exists>no</g:identifier_exists><g:shipping><g:country>PL</g:country><g:service>Darmowa dostawa</g:service><g:price>0.00 PLN</g:price></g:shipping></item>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:g="http://base.google.com/ns/1.0"><channel><title>POLMECH.TECH — katalog łuparek</title><link>https://polmech.tech</link><description>Aktualny katalog POLMECH.TECH</description>${items}</channel></rss>`, { headers: { "Content-Type": "application/xml; charset=utf-8", "Cache-Control": "public, s-maxage=1800" } }); }
