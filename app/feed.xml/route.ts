import { getGearboxOffers, getOfferPath, getProductDescription } from "@/lib/allegroOffers";
export const dynamic = "force-dynamic";
const esc = (value: unknown) => String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
export async function GET() { const products = await getGearboxOffers(); const items = products.map((product) => `<item><title>${esc(product.name)}</title><link>https://polmech.tech${getOfferPath(product)}</link><guid isPermaLink="true">https://polmech.tech${getOfferPath(product)}</guid><description>${esc(getProductDescription(product))}</description></item>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>POLMECH.TECH — aktualne oferty</title><link>https://polmech.tech</link><description>Łuparki przekładniowe i części POLMECH.TECH</description>${items}</channel></rss>`, { headers: { "Content-Type": "application/rss+xml; charset=utf-8", "Cache-Control": "public, s-maxage=1800" } }); }
