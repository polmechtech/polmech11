import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getSeoGuide, seoGuides } from "@/lib/seoGuides";
export const revalidate = 3600;
export const generateStaticParams = () => seoGuides.map(({ slug }) => ({ slug }));
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const guide = getSeoGuide((await params).slug); return guide ? { title: guide.title, description: guide.description, alternates: { canonical: `/poradnik/${guide.slug}` } } : { robots: { index: false } }; }
export default async function GuidePage({ params }: { params: Promise<{ slug: string }> }) { const guide = getSeoGuide((await params).slug); if (!guide) notFound(); const url = `https://polmech.tech/poradnik/${guide.slug}`; const jsonLd = [{ "@context": "https://schema.org", "@type": "Article", headline: guide.heading, description: guide.description, datePublished: "2026-09-04", dateModified: "2026-09-04", mainEntityOfPage: url, author: { "@type": "Organization", name: "POLMECH.TECH", url: "https://polmech.tech" }, publisher: { "@type": "Organization", name: "POLMECH.TECH", url: "https://polmech.tech" } }, { "@context": "https://schema.org", "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "POLMECH.TECH", item: "https://polmech.tech" }, { "@type": "ListItem", position: 2, name: "Poradnik", item: "https://polmech.tech/poradnik" }, { "@type": "ListItem", position: 3, name: guide.heading, item: url }] }]; return <main className="min-h-screen bg-black px-4 py-10 text-white sm:px-6"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><article className="mx-auto max-w-4xl rounded-3xl border border-white/10 bg-neutral-950 p-6 sm:p-10"><Link href="/poradnik" className="font-bold text-red-400">← Wszystkie poradniki</Link><h1 className="mt-6 text-4xl font-black leading-tight sm:text-5xl">{guide.heading}</h1><p className="mt-5 text-xl leading-8 text-neutral-300">{guide.description}</p>{guide.sections.map((section) => <section key={section.heading} className="mt-10"><h2 className="text-2xl font-black">{section.heading}</h2><p className="mt-4 text-lg leading-8 text-neutral-300">{section.text}</p></section>)}<nav aria-label="Powiązane strony" className="mt-10 flex flex-wrap gap-3 border-t border-white/10 pt-7">{guide.links.map((link) => <Link key={link.href} href={link.href} className="rounded-full bg-red-600 px-5 py-3 font-bold text-white">{link.label}</Link>)}</nav></article></main>; }
