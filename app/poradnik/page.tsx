import type { Metadata } from "next";
import Link from "next/link";
import { seoGuides } from "@/lib/seoGuides";
export const revalidate = 3600;
export const metadata: Metadata = { title: "Poradnik o łuparkach przekładniowych", description: "Dobór, eksploatacja i bezpieczeństwo łuparek przekładniowych POLMECH.TECH.", alternates: { canonical: "/poradnik" } };
export default function GuidesPage() { const jsonLd = { "@context": "https://schema.org", "@type": "CollectionPage", name: "Poradnik POLMECH.TECH", url: "https://polmech.tech/poradnik", mainEntity: { "@type": "ItemList", itemListElement: seoGuides.map((guide, index) => ({ "@type": "ListItem", position: index + 1, name: guide.heading, url: `https://polmech.tech/poradnik/${guide.slug}` })) } }; return <main className="min-h-screen bg-black px-4 py-10 text-white sm:px-6"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><div className="mx-auto max-w-5xl"><Link href="/" className="font-bold text-red-400">← POLMECH.TECH</Link><h1 className="mt-6 text-4xl font-black sm:text-6xl">Poradnik o łuparkach przekładniowych</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-neutral-300">Dobór konstrukcji, zasilania i podzespołów oraz bezpieczna eksploatacja łuparki do drewna.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{seoGuides.map((guide) => <article key={guide.slug} className="rounded-3xl border border-white/10 bg-neutral-950 p-6"><h2 className="text-2xl font-black">{guide.heading}</h2><p className="mt-3 leading-7 text-neutral-300">{guide.description}</p><Link href={`/poradnik/${guide.slug}`} className="mt-5 inline-flex font-black text-red-400">Czytaj poradnik →</Link></article>)}</div></div></main>; }
